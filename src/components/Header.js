import React from 'react'
import logo from '../assets/img/logo.png';

const Header = () => {
  return (
    <header>
        Header
        <img src={logo} />
    </header>
  )
}

export default Header